# Backstage catalog entry, read by paved's scorecard plugin.
#
# Every check paved scores is derived from this file alone — no cluster
# access, no CI credentials. What is absent here is absent from the score.
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: terraform-helm-kepler
  description: >-
      Terraform module to deploy Kepler (Kubernetes Environmental Power Profiling Operator) on Kubernetes
      using Helm.
  annotations:
    github.com/project-slug: fabiocicerchia/terraform-helm-kepler
    backstage.io/source-location: url:https://github.com/fabiocicerchia/terraform-helm-kepler/
  links:
    - url: https://github.com/fabiocicerchia/terraform-helm-kepler
      title: Source
spec:
  type: library
  lifecycle: production
  owner: group:default/kodebeat
  system: oss-tools
